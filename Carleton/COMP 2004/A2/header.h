#ifndef HEADER_H
#define HEADER_H

#include <iostream>
#include <string>
#include <sstream>
#include <limits>
#include <cstdlib> //srand(), rand(), NULL
#include <ctime> //time()
using namespace std; 
#include "Cell.h"
#include "Inspector.h"

int randomInt(const int lower = -1, const int upper = 1);

#endif
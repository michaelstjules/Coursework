Michael St. Jules

A2/
	A2.c
	readme.txt

Compile with "gcc -o A2 A2.c" and then run with "./A2".
Test the program before reading the code.
In particular, type invalid input (not 0, 1, 2, 3, 4 or 5).